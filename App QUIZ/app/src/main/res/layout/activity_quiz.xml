<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#FFFFFF">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/tvScore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Điểm: 0"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvTimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:text="15"
            android:textColor="#FF0000"
            android:textSize="18sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <ProgressBar
        android:id="@+id/progressBarTimer"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="100"
        android:layout_marginBottom="24dp" />

    <TextView
        android:id="@+id/tvQuestion"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Nội dung câu hỏi đố vui..."
        android:textSize="20sp"
        android:gravity="center"
        android:textColor="#212121"
        android:layout_marginBottom="24dp"/>

    <Button android:id="@+id/btnOptionA" android:layout_width="match_parent" android:layout_height="55dp" android:text="Đáp án A" android:layout_marginBottom="8dp" android:textColor="#000000" android:backgroundTint="#E0E0E0"/>
    <Button android:id="@+id/btnOptionB" android:layout_width="match_parent" android:layout_height="55dp" android:text="Đáp án B" android:layout_marginBottom="8dp" android:textColor="#000000" android:backgroundTint="#E0E0E0"/>
    <Button android:id="@+id/btnOptionC" android:layout_width="match_parent" android:layout_height="55dp" android:text="Đáp án C" android:layout_marginBottom="8dp" android:textColor="#000000" android:backgroundTint="#E0E0E0"/>
    <Button android:id="@+id/btnOptionD" android:layout_width="match_parent" android:layout_height="55dp" android:text="Đáp án D" android:layout_marginBottom="16dp" android:textColor="#000000" android:backgroundTint="#E0E0E0"/>
</LinearLayout>